# Keep at least one backend serving during voluntary disruptions (node drain).
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: backend
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: backend
